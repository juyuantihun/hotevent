现在有如下需求，先对这些需求进行拆解，然后进行详细设计，最后进行前端页面设计，页面设计要美观大方。
1、技术栈使用java17、springboot3.0、mybatis-plus3.5、mysql5.7、hutool5.8、deepseek、VUE3等前端技术
2、需要使用deepseek定时从网络上的可靠新源获取国际热点事件，获取的数据格式遵循GDELT，包括事件ID、事件时间、事件地点、事件类型、事件描述、
主体、客体、类型、经度、纬度、关键词等字段。
3、也需要有人工录入事件的页面，事件的字段与deepseek获取的数据字段一致。但是可以一次录入多个事件，可以给多个事件（包括人工录入和DEEPSEEK获取的事件）两两之间建立关联关系。
-举例：以色列袭击伊朗可能导致黎巴嫩袭击以色列。这个就是2个事件，事件1是以色列袭击伊朗，事件2是黎巴嫩袭击以色列。事件1和事件2之间建立关联关系，关联关系是“导致”。
-事件录入时，主体、客体、类型、经度、纬度、关键词等字段可以手动录入，事件描述字段可以自动生成，生成规则是：事件主体+事件类型+事件客体。事件描述字段也可以手动录入。
4、建立字典管理模块，事件中的主体、客体、类型等字段需要从字典中获取，字典包括：国家、地区、城市、事件类型、事件主体、事件客体等。
5、如果录入事件或获取事件时，发现事件主体、客体、类型等字段不在字典中，需要自动将新字段添加到字典中。


新增需求如下，请先解析需求，再进行开发：
1、字典中的国家、组织、人物等实际上是一个个的实体，有其各自的属性，比如国家有名称、简称、人口、面积、首都、语言、货币等属性，组织有名称、简称、成立时间、
总部地点、类型、规模等属性，人物有名称、性别、出生日期、逝世日期、国籍、职业等属性。
2、实体和实体之间可以建立关系，比如国家与国家之间可以建立“邻国”、“盟友”、“支持”等关系，国家与城市之间可以建立“首都”等关系，国家与人物之间可以建立“国籍”、“通缉”等关系，组织与人物之间可以建立“创始人”、“隶属”等关系。

1、添加事件的关联关系设置需要添加如下字段：关系名称（必填）、强度级别（选填）。比如：关系名称为“导致”，强度级别为“3”。


录入事件页面，事件地点、事件类型、事件主体、事件客体都是通过字典管理配置的，也是从字典管理中获取。
这个数据应该是从dictionary表查询的@hot_events_db.sql 如事件类型是dictionary表中dict_type=事件类型的数据，
下拉框显示dict_code。事件主体的dict_type=事件主体、事件客体dict_type=事件客体。地点dict_type in（国家、地区、城市）等。
可以调用“根据类型获取字典项”接口获取数据，接口地址为：/api/dictionary/type/{dictType}，dictType为字典类型，比如事件类型、事件主体、事件客体、国家、地区、城市等。
数据库已经有相应的数据了，dict_code字段存储的就是中文


新增需求如下，请先解析需求，再进行开发：
1、新增事件和事件关联组成事件线的算法，通过调用deepseek R1模型，获取事件之间的关联关系
2、可以参考引用中的两个文档和图片中的样式
3、事件和事件的关系，通过event表的event_code进行关联，保存到neo4j数据库，账号是neo4j,密码是1qaz2wsx
4、最后需要在页面上以图的方式展示

1、现在的事件与事件的关系是两两调用deepseek来判断是否存在关联，这种方式效率太低，
应该是一次用100个甚至更多（deepseek允许的前提下）事件，直接交给deepseek进行关联。

新增需求如下，请先解析需求，再进行开发：
1、deepseek搜索事件，得到的主体、客体、类型等字段，需要添加到字典管理中，如果字典管理中已经存在，则不添加。
2、由于事件大部分是从网络上使用deepseek搜索得到，难免会有重复的，所以在建立时间线的时候，需要先对事件进行去重，
去重规则是：事件主体、事件客体、事件类型、事件时间、事件地点都相同，则认为是重复事件。如果是重复事件，则只保留一条，其他删除。
3、根据事件的时间为主线，生成事件时间线，事件时间线包括事件本身和事件之间的关联关系。事件时间线需要保存到neo4j数据库，账号是neo4j,密码是1qaz2wsx,同时保存到mysql数据库中。
4、两个事件之间的关系，比如：以色列袭击伊朗可能导致黎巴嫩袭击以色列。这个就是2个事件，事件1是以色列袭击伊朗，
事件2是黎巴嫩袭击以色列。事件1和事件2之间建立关联关系，关联关系是“导致”。一个事件可能导致多个事件，一个事件也可能被多个事件导致。
所以分析的时候需要将所有事件全部发送给deepseek进行关联分析。事件与事件之间有各种关系，并不只有导致一种，所以需要你们根据deepseek返回的数据，分析出事件与事件之间的关系，并保存到数据库中。
5、需要将事件时间线以图的方式展示出来，展示的时候需要将事件和事件之间的关系展示出来，比如：以色列袭击伊朗可能导致黎巴嫩袭击以色列。这个就是2个事件，事件1是以色列袭击伊朗，事件2是黎巴嫩袭击以色列。事件1和事件2之间建立关联关系，关联关系是“导致”。这个需要展示出来。


现在的事件时间线生成逻辑有问题，我需要重新调整，你帮我梳理一下：
1、当前的页面是timeline/list，页面上的卡片是需要从数据库查询，这个数据库表似乎没有设计
2、点击到卡片详情后，里面的内容也应该是数据库查询的，这个数据库表似乎没有设计
3、生成时间线弹窗，不应该让我选择弹窗，而是可以选择一个地区，从数据库获取所有与该地区相关的事件后，生成相应的时间线


1、地区不止是一个地名，可以包含很多国家和地区，是一个数组，地区需要有专门的页面进行管理。
2、在生成事件时间线时，选择已经创建好的地区，允许在生成时间线的编辑页面临时添加地区。
3、生成时间线需要有起止时间，在这个时间段内，通过deepseek v1的推理模型，讲该地区所有能在网络上获取到的事件放在一起分析，是同一个或相关的事件的，放在同一个事件时间线里。允许在同一时间段同一地区生成多条时间线。
4、还有我没想到的，你也可以给我补充


现在生成时间线没有调用deepseek了，需要去调用。且调用deepseek的提示词需要修改，逻辑如下：
1、请动态生成提示词，提示词的模板需要写入单独的配置文件，每次调用deepseek从配置文件读取，然后根据前端参数生成对应的提示词。
2、前端的入参是时间线名称、时间线描述、地区、起止时间。
3、需要从数据库或使用deepseek从网络上检索这段时间内相关国家/地区与标题和描述相关的事件，然后编制成时间线。
4、如果事件是从网络上检索的，需要保证事件的真实性，并讲检索的到的事件存入数据库
